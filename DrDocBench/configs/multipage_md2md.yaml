end2end_eval:
  metrics:
    text_block:
      metric:
        - Edit_dist
    display_formula:
      metric:
        - Edit_dist
        - CDM_plain
    table:
      metric:
        - TEDS
        - Edit_dist
    reading_order:
      metric:
        - Edit_dist
  dataset:
    dataset_name: multipage_md2md_dataset
    ground_truth:
      data_path: /root/close_source_benchmark   # or /root/open_source
    prediction:
      data_path: ../../0423-results/gemini31-multipage/close_source
    match_method: quick_match
    include_subjects:
      - MUSIC   # whole-doc MusicXML evaluation; run separately from multipage_end2end.yaml
